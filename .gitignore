node_modules
dist
.DS_Store
*.log
# Wrangler local state (build cache, secrets, dev session)
.wrangler
.dev.vars

# Claude Code: ignore session runtime, but track the committed tooling
# (skills, hooks, settings) the repo + its CI depend on.
.claude/*
!.claude/skills/
!.claude/hooks/
!.claude/settings.json
