# DSA (DeepSeek Sparse Attention) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add `'mla-dsa'` as a new variant on `AttentionConfig` to model DeepSeek V3.2's sparse attention. KV cache stays full-shape; attention compute caps at `topK`. Add DeepSeek V3.2 as the canonical user.

**Architecture:** New discriminated-union variant. Three branched helpers (`kvBytesPerTokenPerLayer`, `attentionDim`, `attendedSeqlenSummedOverLayers`) gain an `'mla-dsa'` branch — KV/attentionDim delegate to MLA logic, attendedSeq caps at `layers × min(seqlen, topK)`. Indexer overhead is footnoted, not modeled (production V3.2 quantizes the indexer).

**Tech Stack:** TypeScript, Svelte 5, Vitest. No new deps.

**Spec:** `calc/docs/superpowers/specs/2026-05-12-dsa-design.md`

---

## File Structure

```
src/engine/
  types.ts        # add 'mla-dsa' variant to AttentionConfig
  memory.ts       # add 'mla-dsa' branch to kvBytesPerTokenPerLayer, attentionDim,
                  # attendedSeqlenSummedOverLayers
src/data/
  models.ts       # add DeepSeek V3.2 entry
test/engine/
  sliding.test.ts # add helper tests for 'mla-dsa' branch in each of the 3 helpers
  memory.test.ts  # add memory KV-cache test (DSA uses MLA shape, full seqlen)
  prefill.test.ts # add prefill attention term test (topK-capped attendedSeq)
  decode.test.ts  # add decode attention term test (topK-capped attendedSeq)
  calc.test.ts    # add V3.2 integration test (vs V3 comparison)
```

---

## Task 1: Schema (add 'mla-dsa' variant)

Pure type addition. Existing entries (`'full'`, `'sliding'`, `'mla'`, `'hybrid'`) remain valid when the union grows.

**Files:**
- Modify: `calc/src/engine/types.ts`

- [ ] **Step 1: Add the variant to AttentionConfig**

In `calc/src/engine/types.ts`, locate `AttentionConfig` (currently lines 31-35):

```ts
export type AttentionConfig =
  | { type: 'full' }
  | { type: 'sliding'; window: number }
  | { type: 'mla'; kvLoraRank: number; qkRopeHeadDim: number }
  | { type: 'hybrid'; slidingWindow: number; numSlidingLayers: number; numGlobalLayers: number }
```

Replace with:

```ts
export type AttentionConfig =
  | { type: 'full' }
  | { type: 'sliding'; window: number }
  | { type: 'mla'; kvLoraRank: number; qkRopeHeadDim: number }
  | { type: 'hybrid'; slidingWindow: number; numSlidingLayers: number; numGlobalLayers: number }
  | { type: 'mla-dsa'; kvLoraRank: number; qkRopeHeadDim: number; topK: number }
```

- [ ] **Step 2: Run check + tests**

```bash
cd /Users/yao/workspace/llm-perf/calc
npm run check
npm test
```

Expected: type-check clean. All 69 existing tests PASS (no model uses the new variant).

- [ ] **Step 3: Commit**

```bash
cd /Users/yao/workspace/llm-perf
git add calc/src/engine/types.ts
git commit -m "feat(calc): add 'mla-dsa' variant to AttentionConfig"
```

No Co-Authored-By footer.

---

## Task 2: Helpers — add 'mla-dsa' branch to all three (TDD)

Three helpers each get one new branch. Behavior for `'mla-dsa'`:
- `kvBytesPerTokenPerLayer`: same as `'mla'` (DSA doesn't change KV size)
- `attentionDim`: same as `'mla'` (DSA doesn't change attention representation)
- `attendedSeqlenSummedOverLayers`: NEW — `layers × min(seqlen, topK)` (sparse attention cap)

**Files:**
- Modify: `calc/test/engine/sliding.test.ts`
- Modify: `calc/src/engine/memory.ts`

- [ ] **Step 1: Append failing tests to `test/engine/sliding.test.ts`**

Inside the existing `describe('kvBytesPerTokenPerLayer', ...)` block, append (just before its closing `})`):

```ts
  it('mla-dsa: same as MLA (DSA does not change KV size)', () => {
    const dsa: ModelArch = {
      ...base,
      attention: { type: 'mla-dsa', kvLoraRank: 32, qkRopeHeadDim: 8, topK: 16 }
    }
    // (32 + 8) × 2 = 80
    expect(kvBytesPerTokenPerLayer(dsa, 'fp16')).toBe(80)
  })
```

Inside `describe('attentionDim', ...)`, append (just before its closing `})`):

```ts
  it('mla-dsa: same as MLA (DSA does not change attention representation)', () => {
    const dsa: ModelArch = {
      ...base,
      attention: { type: 'mla-dsa', kvLoraRank: 32, qkRopeHeadDim: 8, topK: 16 }
    }
    expect(attentionDim(dsa)).toBe(40)
  })
```

Inside `describe('attendedSeqlenSummedOverLayers', ...)`, append (just before its closing `})`):

```ts
  it('mla-dsa: layers × min(seqlen, topK)', () => {
    const m: ModelArch = {
      ...base,
      attention: { type: 'mla-dsa', kvLoraRank: 32, qkRopeHeadDim: 8, topK: 50 }
    }
    // seqlen > topK: 4 × 50 = 200
    expect(attendedSeqlenSummedOverLayers(m, 100)).toBe(200)
    // seqlen < topK: 4 × 30 = 120
    expect(attendedSeqlenSummedOverLayers(m, 30)).toBe(120)
    // seqlen == topK: 4 × 50 = 200
    expect(attendedSeqlenSummedOverLayers(m, 50)).toBe(200)
  })
```

- [ ] **Step 2: Run tests; expect 3 new failures**

```bash
cd /Users/yao/workspace/llm-perf/calc
npm test -- test/engine/sliding.test.ts
```

Expected: 3 new tests FAIL — the helpers don't have an `'mla-dsa'` branch yet, so they fall through to the non-MLA path producing wrong values.

- [ ] **Step 3: Add the 'mla-dsa' branches to the three helpers in `memory.ts`**

In `calc/src/engine/memory.ts`:

Update `kvBytesPerTokenPerLayer` (currently lines 12-18):

```ts
export function kvBytesPerTokenPerLayer(model: ModelArch, kvDtype: Dtype): number {
  const att = model.attention
  if (att.type === 'mla' || att.type === 'mla-dsa') {
    return (att.kvLoraRank + att.qkRopeHeadDim) * bytesOf(kvDtype)
  }
  return 2 * model.numKvHeads * model.headDim * bytesOf(kvDtype)
}
```

Update `attentionDim` (currently lines 20-24):

```ts
export function attentionDim(model: ModelArch): number {
  const att = model.attention
  if (att.type === 'mla' || att.type === 'mla-dsa') return att.kvLoraRank + att.qkRopeHeadDim
  return model.numHeads * model.headDim
}
```

Update `attendedSeqlenSummedOverLayers` (currently lines 26-40) — add the `mla-dsa` branch BEFORE the `sliding/full/mla` fallthrough at the bottom:

```ts
export function attendedSeqlenSummedOverLayers(model: ModelArch, seqlen: number): number {
  const att = model.attention
  if (att.type === 'hybrid') {
    if (att.numSlidingLayers + att.numGlobalLayers !== model.layers) {
      throw new Error(
        `hybrid layer counts must sum to model.layers: ` +
        `${att.numSlidingLayers} + ${att.numGlobalLayers} ≠ ${model.layers}`
      )
    }
    return att.numSlidingLayers * Math.min(seqlen, att.slidingWindow)
         + att.numGlobalLayers * seqlen
  }
  if (att.type === 'mla-dsa') return model.layers * Math.min(seqlen, att.topK)
  const perLayer = att.type === 'sliding' ? Math.min(seqlen, att.window) : seqlen
  return model.layers * perLayer
}
```

- [ ] **Step 4: Run tests and type-check**

```bash
cd /Users/yao/workspace/llm-perf/calc
npm test
npm run check
```

Expected: all 72 tests PASS (69 existing + 3 new). Type-check clean.

- [ ] **Step 5: Commit**

```bash
cd /Users/yao/workspace/llm-perf
git add calc/src/engine/memory.ts calc/test/engine/sliding.test.ts
git commit -m "feat(calc): add 'mla-dsa' branch to kvBytesPerTokenPerLayer, attentionDim, attendedSeqlenSummedOverLayers"
```

No Co-Authored-By footer.

---

## Task 3: Wiring regression tests in memory / prefill / decode

Three regression-lock tests with a synthetic `'mla-dsa'` fixture, one per call site. They should pass without further code changes — confirming the helpers are wired in correctly at the three call sites (already done in earlier features).

**Files:**
- Modify: `calc/test/engine/memory.test.ts`
- Modify: `calc/test/engine/prefill.test.ts`
- Modify: `calc/test/engine/decode.test.ts`

- [ ] **Step 1: Append the MLA-DSA KV cache test to `memory.test.ts`**

Append inside `describe('computeMemory', ...)` (just before its closing `})`):

```ts
  it('kvCachePerRequest uses MLA formula for mla-dsa (DSA does not shrink KV)', () => {
    // testModel: layers=2, fp16 KV; prompt+output=15.
    // MLA-DSA with kvLoraRank=10, rope=2, topK=4:
    //   per-layer KV bytes = (10+2) × 2 = 24
    //   attendedSeqlen = 2 × 15 = 30  (KV unaffected by topK — every past token cached)
    //   kvCachePerRequest = 24 × 30 = 720
    const dsaModel = {
      ...testInput.model,
      attention: { type: 'mla-dsa' as const, kvLoraRank: 10, qkRopeHeadDim: 2, topK: 4 }
    }
    const input = { ...testInput, model: dsaModel }
    const m = computeMemory(input)
    expect(m.kvCachePerRequest).toBe(720)
    // × concurrency 2 = 1440 bytes total
    expect(m.kvCacheTotal).toBe(1440)
  })
```

- [ ] **Step 2: Append the MLA-DSA prefill test to `prefill.test.ts`**

Append inside `describe('computePrefill', ...)`:

```ts
  it('attention term caps at topK for mla-dsa', () => {
    // testModel: layers=2, paramCount=1000, prompt=10.
    // MLA-DSA with kvLoraRank=10, rope=2 → attentionDim=12; topK=3 (< prompt=10).
    //   attendedSeq = 2 × min(10, 3) = 6
    //   MLP: 2 × 1000 × 10 = 20000
    //   Attention: 2 × 10 × 6 × 12 = 1440
    //   Total = 21440
    const dsaModel = {
      ...testInput.model,
      attention: { type: 'mla-dsa' as const, kvLoraRank: 10, qkRopeHeadDim: 2, topK: 3 }
    }
    const input = { ...testInput, model: dsaModel }
    const dsaMemory = computeMemory(input)
    const p = computePrefill(input, opPoint, dsaMemory)
    expect(p.flops).toBe(21440)
  })
```

- [ ] **Step 3: Append the MLA-DSA decode test to `decode.test.ts`**

Append inside `describe('computeDecode', ...)`:

```ts
  it('attention term caps at topK for mla-dsa', () => {
    // testModel: layers=2, paramCount=1000, concurrency=2.
    // avgSeqlen = 10 + 5/2 = 12.5.
    // MLA-DSA with kvLoraRank=10, rope=2 → attentionDim=12; topK=4 (< 12.5).
    //   attendedSeq = 2 × min(12.5, 4) = 8
    //   flopsPerStep = (2×1000 + 2×8×12) × 2 = (2000 + 192) × 2 = 4384
    const dsaModel = {
      ...testInput.model,
      attention: { type: 'mla-dsa' as const, kvLoraRank: 10, qkRopeHeadDim: 2, topK: 4 }
    }
    const input = { ...testInput, model: dsaModel }
    const dsaMemory = computeMemory(input)
    const d = computeDecode(input, opPoint, dsaMemory)
    expect(d.flopsPerStep).toBe(4384)
  })
```

- [ ] **Step 4: Run tests and type-check**

```bash
cd /Users/yao/workspace/llm-perf/calc
npm test
npm run check
```

Expected: all 75 tests PASS (72 + 3 new). If any of the three new tests fails, the wiring helpers may be missing the `'mla-dsa'` branch — re-read Task 2.

- [ ] **Step 5: Commit**

```bash
cd /Users/yao/workspace/llm-perf
git add calc/test/engine/memory.test.ts calc/test/engine/prefill.test.ts calc/test/engine/decode.test.ts
git commit -m "test(calc): mla-dsa wiring in memory, prefill, decode"
```

No Co-Authored-By footer.

---

## Task 4: DeepSeek V3.2 data entry + integration test

Adds V3.2 as canonical user. Integration test compares V3 vs V3.2 at the same workload — KV cache identical, prefill attention term smaller by `seqlen/topK` ratio.

V3.2 config (verified against `deepseek-ai/DeepSeek-V3.2/config.json`):
- 61 layers, hidden 7168, intermediate 18432
- 128 heads, 128 KV heads, vocab 129280
- 256 routed + 1 shared, 8 active per token
- MLA: kv_lora_rank 512, qk_rope_head_dim 64 (same as V3)
- DSA: index_topk 2048

paramCount / activeParamCount: V3.2 adds ~3.6B indexer params on top of V3's 671B (sub-1% delta). Model card rounds to the same 671B / 37B figures — we follow.

**Files:**
- Modify: `calc/src/data/models.ts`
- Modify: `calc/test/engine/calc.test.ts`

- [ ] **Step 1: Add the DeepSeek V3.2 entry**

In `calc/src/data/models.ts`, after the DeepSeek V3 entry (still within the `// === DeepSeek ===` family block), insert:

```ts
  {
    id: 'deepseek-v3.2', name: 'DeepSeek-V3.2', family: 'deepseek',
    layers: 61, hiddenDim: 7168, intermediateDim: 18432,
    numHeads: 128, numKvHeads: 128, headDim: 192, vocabSize: 129280,
    paramCount: 671_000_000_000,
    attention: { type: 'mla-dsa', kvLoraRank: 512, qkRopeHeadDim: 64, topK: 2048 },
    architecture: {
      type: 'moe',
      numExperts: 256,
      numExpertsActive: 8,
      numSharedExperts: 1,
      activeParamCount: 37_000_000_000
    }
  },
```

- [ ] **Step 2: Append the V3.2 integration test**

In `calc/test/engine/calc.test.ts`, append a new top-level describe block at the bottom:

```ts
describe('calculate — DeepSeek V3.2 (DSA) integration', () => {
  const h100 = GPUS.find(g => g.id === 'h100')!
  const v3 = MODELS.find(m => m.id === 'deepseek-v3')!
  const v32 = MODELS.find(m => m.id === 'deepseek-v3.2')!

  const baseInput: Omit<CalcInput, 'model'> = {
    gpu: h100,
    gpuVariantId: 'sxm-80',
    quant: { weights: 'fp16', kv: 'fp16', activations: 'fp16' },
    workload: { promptTokens: 32768, outputTokens: 0, concurrency: 1 }
  }

  it('V3.2 KV cache at 32k is identical to V3 (DSA does not shrink KV)', () => {
    const r3 = calculate({ ...baseInput, model: v3 })
    const r32 = calculate({ ...baseInput, model: v32 })
    expect(r32.memory.kvCachePerRequest).toBe(r3.memory.kvCachePerRequest)
    expect(r32.memory.kvCachePerRequest).toBe(61 * (512 + 64) * 2 * 32768)
  })

  it('V3.2 prefill attention term shrinks by ratio seqlen/topK ≈ 16× vs V3', () => {
    const r3 = calculate({ ...baseInput, model: v3 })
    const r32 = calculate({ ...baseInput, model: v32 })
    // MLP term is identical (same activeParams, same prompt). Attention term differs.
    // Both share the same MLP term: 2 × 37e9 × 32768 = 2.4248832e15
    const mlpTerm = 2 * 37_000_000_000 * 32768
    const v3AttentionTerm = r3.perf['peak'].prefill.flops - mlpTerm
    const v32AttentionTerm = r32.perf['peak'].prefill.flops - mlpTerm
    // V3:   attendedSeq = 61 × 32768 = 1_998_848
    // V3.2: attendedSeq = 61 × 2048  = 124_928 (capped at topK)
    // Ratio: 32768 / 2048 = 16
    expect(v3AttentionTerm / v32AttentionTerm).toBeCloseTo(32768 / 2048, 6)
  })
})
```

- [ ] **Step 3: Run tests + type-check**

```bash
cd /Users/yao/workspace/llm-perf/calc
npm test
npm run check
```

Expected: 77 tests PASS (75 from Task 3 + 2 V3.2 integration cases). Type-check clean.

- [ ] **Step 4: Commit**

```bash
cd /Users/yao/workspace/llm-perf
git add calc/src/data/models.ts calc/test/engine/calc.test.ts
git commit -m "feat(calc): add DeepSeek V3.2 (MLA-DSA with topK=2048)"
```

No Co-Authored-By footer.

---

## Done

After Task 4:
- Branch `feat/calc-dsa` has 4 implementation commits on top of the spec commit
- 77 / 77 tests pass
- `npm run check` clean
- `'mla-dsa'` variant exists; V3.2 populates it
- Controller will offer to open the PR
